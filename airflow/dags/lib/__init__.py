"""Library code imported by the dark_factory_pipeline DAG.

Keeping logic out of the DAG file makes it unit-testable and keeps the DAG
file itself focused on orchestration (tasks, dependencies, schedule).
"""
