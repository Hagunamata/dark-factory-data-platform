# Logstash pipeline: collect container logs and ship to Elasticsearch.
#
# This is a minimal starter config. In Phase 2 it will be extended to parse
# structured log fields from individual services (Kafka, Spark, Airflow).

input {
  # TODO (Phase 2): configure log collection. Options:
  #   - filebeat input from a mounted Docker socket
  #   - gelf input on a UDP port with Docker's gelf log driver
  #   - tcp/udp input from container stdout via docker logging driver
}

filter {
  # TODO (Phase 2):
  #   - extract service name from container labels
  #   - parse JSON-formatted log lines from Airflow and Spark
  #   - drop noisy health-check entries
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "darkfactory-logs-%{+YYYY.MM.dd}"
  }
}
